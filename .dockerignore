# Keep the build context small and, crucially, prevent the host's Windows-native
# node_modules from being copied into the Linux image (it would clobber the
# Linux install and fail at runtime).
node_modules
dist
.git
*.log
